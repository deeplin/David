<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar" />

    <style name="image_button_style" parent="android:Widget.Button">
        <item name="android:layout_width">80dp</item>
        <item name="android:layout_height">@dimen/y40</item>
        <item name="android:background">@drawable/button</item>
    </style>

    <style name="button_style" parent="image_button_style">
        <item name="android:textColor">@color/info_white</item>
        <item name="android:textSize">@dimen/f18</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="large_image_button_style" parent="android:Widget.Button">
        <item name="android:layout_width">100dp</item>
        <item name="android:layout_height">@dimen/y48</item>
        <item name="android:background">@drawable/button</item>
    </style>

    <style name="large_button_style" parent="large_image_button_style">
        <item name="android:background">@drawable/button</item>
        <item name="android:textColor">@color/info_white</item>
        <item name="android:textSize">@dimen/f24</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="dialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        //Dialog的windowFrame框为无
        <item name="android:windowIsFloating">true</item>
        //是否浮现在activity之上
        <item name="android:windowIsTranslucent">true</item>
        //是否半透明
        <item name="android:windowNoTitle">true</item>
        //是否显示title
        <item name="android:background">@android:color/transparent</item>
        //设置dialog的背景
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimAmount">0.7</item>
        //就是用来控制灰度的值，当为1时，界面除了我们的dialog内容是高亮显示的，dialog以外的区域是黑色的，完全看不到其他内容
        <item name="android:backgroundDimEnabled">true</item>
    </style>

    <style name="time_style" parent="@android:style/Theme.Holo.Light">
        <item name="android:textColor">@color/info_white</item>
        <item name="android:textSize">@dimen/f24</item>
    </style>

</resources>